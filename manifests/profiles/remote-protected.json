{
  "acp": "refuse",
  "env_passthrough": [],
  "gateway": "transparent",
  "guarantee": "Model egress, as in protected, with interception scoped to the agent's own control group so the rest of the host is untouched. MCP servers are restricted to a registry allowlist. Other tool network egress is not controlled.",
  "handoff": {
    "system_prompt": false
  },
  "hooks": "enforced",
  "masking_level": "standard",
  "mcp": {
    "strict": true
  },
  "name": "remote-protected",
  "netpolicy": "protected",
  "remote": [
    "claude",
    "codex"
  ],
  "sandbox": "vendor",
  "schema": 1
}
